import React from 'react';

const Cart = ({cart, products}) => {
    console.log(cart)
    // const {name}= cart;
    return (
        <div className='cart-container'>
            {/* <h1>{name}</h1> */}
        </div>
    );
};

export default Cart;